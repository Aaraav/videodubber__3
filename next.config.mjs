/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ph-files.imgix.net', 'ph-static.imgix.net', 'img.freepik.com', 'cdn-icons-png.flaticon.com'],
    },
};

export default nextConfig;
